name: idmcifras_ecossistema_zuri_rimane

description: >-
  IDMCifras — Ecossistema Zuri Rimane: scaffold Flutter multi-platform (web + mobile)

version: 0.1.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: '>=2.10.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  http: ^0.13.6
  cached_network_image: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
