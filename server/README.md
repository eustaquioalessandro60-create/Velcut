# Integration Server (POC)

This small Node/Express server is a proof-of-concept to:
- Store API keys (via ADMIN_TOKEN-protected endpoint)
- Create mock jobs for video/audio generation

NOT for production. For production use a proper secrets manager and job queue.
