# Frontend — Velcut / Zuri Rimane

This folder (`frontend/`) expects the Flutter web build output.
The GitHub Actions workflow builds the Flutter web automatically and deploys to Cloud Run.

## Build locally (optional):
```bash
flutter build web --release

